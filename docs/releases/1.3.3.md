# Podcast Visualizer 1.3.3

Status: released on 2026-09-23 from signed tag `v1.3.3`, source commit
`5d5a026f6e68a7631d01e92478b2681f1afb0e3a`, and build 26.
[Download the notarized Apple Silicon DMG](https://github.com/aindaco1/podcast-visualizer/releases/download/v1.3.3/Podcast-Visualizer-1.3.3-arm64.dmg)
or view [the public release](https://github.com/aindaco1/podcast-visualizer/releases/tag/v1.3.3).

Native transcript approval preserves complete source sentence boundaries,
including questions and closing quotations. Apple's local sentence tokenizer
keeps abbreviations attached to their continuation. Unfinished phrases receive
optional Apple Intelligence advice in batches of six; incomplete or invalid
responses fall back safely. Words, acoustic speaker authority, timing limits,
review edits, and immutable revisions remain protected.

Speech and Apple generation infrastructure now come from the shared
Dust Wave Platform native package. Product policy stays in Podcast Visualizer.
FluidAudio remains pinned to 0.15.5; this release does not claim a new ASR model
or improved recognition accuracy. New runtime provenance binds the Platform
revision while validating older Record-backed runtimes.

Developer-only Jev evaluation uses hash-allowlisted synthetic text. Real audio,
transcripts, and review data stay on the Mac. The app contains no Jev provider
or credentials. See [the evaluation guide](../testing/jev-evaluation.md) and
[shared migration contract](../shared-native-migration.md).

## Validation and limits

- The unchanged 31-case Jev baseline passed after the shared migration: all
  fourteen controls and seventeen generated/local candidates passed, with no
  missing native evidence or exact-check failures.
- Local synthetic two-voice transcription produced identical transcript text,
  words, token timings, confidence, and speaker turns with the released 1.3.2
  helper and migrated helper. Both misrecognized “quieter” as “choir”; review is
  still necessary. Source audio hashes remained unchanged. The baseline used
  an ad-hoc-signed copy because the installed helper requires the app sandbox.
  This comparison is not signed-app acceptance or a speed benchmark.
- Release delta base: the verified public 1.3.2 ZIP, SHA-256
  `e81d7597166375ceca0892ba9d9357bca03126cd400422893130dbdfa0233213`.
- Final expanded Jev run: 22/22 actual candidates passed; all exact checks
  passed and native capture was complete. Fifteen of sixteen controls matched;
  the deliberately combined-sentence control was routed to review (fail 0.53,
  pass 0.45, uncertain 0.02). Inspection confirms that control is bad. The suite
  correctly remains exit 1, not an all-pass judge result. Questions and the
  0.10 margin were not weakened. Exact grouping regressions now catch a split
  abbreviation even when the judge approves it. This known judge limitation
  does not block this release's independently tested grouping policy.
- Local checks: 217 Node tests passed (two opt-in tests skipped), 130 Swift app
  tests, one sidecar progress test, 27 Python tests, dependency audit, workflow
  lint, and source whitespace checks passed. The Python gate used a fresh locked
  environment after the old editable development environment failed collection.
- Node coverage: 76.24% lines, 76.33% branches, 85.17% functions.
- Exact-source [CI 35890110827](https://github.com/aindaco1/podcast-visualizer/actions/runs/35890110827)
  passed core, stable Xcode 26.3 tests/builds, and Xcode 27 compatibility. It
  assembled and attested the app reused by the release workflow.
- [Release 35890912546](https://github.com/aindaco1/podcast-visualizer/actions/runs/35890912546)
  passed signing, app/DMG notarization and stapling, mounted-image checks, size
  budgets, signed update feed/delta, checksums, SBOM, provenance, and publication.
- All eleven public assets were independently downloaded, and all ten checksum
  entries passed. The 227,555,940-byte DMG passed read-only mounted
  layout, code-sign, staple, and Gatekeeper checks. GitHub attestation binds the
  public digest to this tag and release workflow. Independent Ed25519 verification
  passed for the feed, ZIP, and build-25-to-26 delta using the installed app key.
- The public ZIP reports 1.3.3/build 26. Its signed v4 speech manifest and helper
  hash validate against Platform `0affb6c5652611b87947bd87762d8aa17d35ea32`
  and FluidAudio 0.15.5.
- A separate ad-hoc test copy of the downloaded 1.3.3 speech helper also matched
  1.3.2 exactly on the synthetic recording: text, words, tokens, confidence, and
  four speaker turns. The complete CLI analysis produced 86 words, two speakers,
  and twelve review cues while preserving the original source hash. This does
  not substitute for the app's sandboxed workflow acceptance.
- The installed 1.3.2 app found 1.3.3 via **Check for Updates**. After automation
  timed out at **Install Update**, the user confirmed completing installation
  and relaunch. The installed bundle was independently verified as 1.3.3/build
  26; code-sign, staple, and Gatekeeper checks passed, and the reopened app
  showed both models Ready. A verified 1.3.2 rollback app and public evidence
  are retained in the local
  `PodcastVisualizerReleaseValidation/1.3.3-xj5_2m47` cache. No clean-account or
  automatic-launch-prompt acceptance is claimed.
- After publication, the installed signed 1.3.3 app completed a fresh synthetic
  transcription/review/approval/alignment workflow. The user assisted with
  foregrounding the app and selecting the source/new project when automation
  failed; subsequent review, approval, quit/relaunch, and project reopening were
  exercised through the native UI. This closes the workflow gap that remained
  at publication; the earlier updater confirmation alone did not close it.
- The 35.867-second two-voice fixture produced 86 words and twelve draft cues
  with four ambiguous speaker cues. Native review corrected “choir” to “quieter,”
  assigned the ambiguous speakers, split a mixed-speaker cue at 25.040 seconds,
  confirmed speakers, and saved thirteen cues. Approval remained disabled until
  all speakers were assigned and confirmed. Raw speech, speaker evidence, draft,
  and source/prepared audio hashes remained unchanged.
- Native approval produced an immutable seven-cue revision whose concatenated
  text exactly matches the saved review. Automatic alignment placed all 86
  words with zero missing, interpolated, invalid, or projection-issue words.
  After quit/relaunch, opening the project restored **Aligned**, two speakers,
  and seven cues without starting a render. Local evidence is retained under
  `Documents/PodcastVisualizer-1.3.3-signed-lmjx7uzj/acceptance-result.json`.
- Installed-app analysis took 369.554 seconds, largely waiting for active
  Apple Neural Engine model compilation/loading. This first observed load is
  not a warm-run performance baseline. Transcript text matched the earlier
  comparison, but confidence/timings and automatic-speaker settings differed;
  the installed run is not a byte-identical speech-output claim.
- Formatting remains conservative: the approved result retains the short cues
  “Please,” “slight.” and “clear.” All words and speaker boundaries survived;
  this is workflow acceptance, not perfect grouping or real-podcast quality
  acceptance. The short-clip chapter guard refused generation with an actionable
  message, so live chapter-model generation was not qualified on this fixture.
  Render/export acceptance was not rerun as part of this transcription check.

The immutable signed feed contains preparation-time notes. This document and
the GitHub release description carry the later acceptance record.
