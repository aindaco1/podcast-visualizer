# Podcast Visualizer 1.3.1

Status: prepared for signed tag `v1.3.1`. Publication, signed artifacts, and
installed-app acceptance remain pending until recorded below.

This maintenance release fixes Split at Playhead crashes caused by stale text
selections and rendering failures on transcripts containing pauses longer than
ten seconds. Failed and cancelled renders preserve the prior workflow stage,
aligned data, and existing outputs so rendering can be retried immediately.

Render diagnostics now retain selected options, the last output and phase,
bounded progress, helper exit details, and privacy-safe failure categories.
Recent native crash summaries can be reviewed alongside workflow failures.
**Report a Problem** shows the exact summary before **Send Reviewed Reports**
submits it to the shared Dust Wave relay. Similar reports update one GitHub
issue. Media, transcripts, model inputs, review data, file paths, and raw crash
reports remain local.

New speaker analyses count overlapping coverage once and keep ambiguous spans
available for review. Existing saved analyses are unchanged. Broader model
quality changes still require a local, representative benchmark.

## Source and migration evidence

- Regression coverage includes stale Unicode selection after split/merge/Undo,
  long acoustic gaps, real opaque and transparent video encoding, render retry,
  diagnostic privacy and failure presentation, report review/offline retry,
  native crash projection, and relay grouping/idempotency.
- The shared `@dustwave/timed-text` dependency moves from `0.11.0` at
  `9eac8c0564f536048a7ac29ef96ed01f6d2b856b` to `0.11.1` at
  `6da7db0`. This extends valid acoustic gaps to the supplied recording duration;
  it does not change valid earlier presentation results or policy versions.
  Rollback restores the previous submodule pin and exact package version.
- The reporter's original 1.3.0 logs lack the phase and options needed to prove
  their exact render trigger. The reproduced long-pause failure is fixed;
  expanded diagnostics cover future failures.

See the [investigation](../testing/2026-09-render-and-split.md) and
[diagnostic contract](../support-diagnostics.md).

## Release acceptance

- Shared relay: deployment and synthetic issue aggregation pending.
- Exact-source hosted CI and signed release workflow: pending.
- Public checksums, signing, notarization, DMG readback, and appcast: pending.
- Installed 1.3.0 to 1.3.1 Sparkle replacement and relaunch: pending.
- Cleanup of superseded local builds and merged branches follows acceptance.
