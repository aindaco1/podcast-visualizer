# Podcast Visualizer 1.3.2

Status: released on 2026-09-07 from signed tag `v1.3.2`, source commit
`7aa429f477350633bf1aaafe48afd70ce1d88cf7`, and build 25.
[Download the notarized Apple Silicon DMG](https://github.com/aindaco1/podcast-visualizer/releases/download/v1.3.2/Podcast-Visualizer-1.3.2-arm64.dmg)
or review the [public release](https://github.com/aindaco1/podcast-visualizer/releases/tag/v1.3.2).

Click between words in Transcript Review to move the playhead and pause audio,
then use **Split at Playhead**. The position is estimated from the caret's
location within the cue; use the audio controls to fine-tune before splitting.
Review cue timing does not provide verified word-to-text alignment.

Typing, Find, keyboard navigation, and drag selection leave playback alone.
Repeated clicks at the same caret still reposition the playhead after a manual
transport adjustment. Navigation preserves transcript text, cue timing, Checked
state, and existing approved revisions, alignment, and renders. Missing audio
explains how to save edits and reopen the project safely.

The native click observer retains the existing SwiftUI text editor and exact
text-snapshot selection safeguards. It waits for AppKit text tracking to finish,
accepts events only within its text box, and cancels deferred clicks on teardown.
No review schema, model, shared-package revision, or media pipeline changes are
required.

## Validation

- Regression coverage includes native click tracking without a dispatched
  mouse-up, Unicode caret estimates, stale selections, repeated and modified
  clicks, drag selection, teardown, outside clicks, text editing, Find, missing
  audio recovery, and splitting at a manually refined playhead.
- A local synthetic preview verified text-click seeking, click-and-split,
  drag selection without seeking, and a text click pausing active playback.
- The update delta uses the verified public 1.3.1/build 24 archive as its base.
- Local release gates passed 188 Node tests (two opt-in gates skipped),
  121 app tests, the speech-sidecar suite, 27 Python tests, the dependency audit,
  workflow lint, and the assembled app's complete bundled-runtime validation.
  Node coverage is 75.31% of lines, 73.93% of branches, and 84.22% of functions.
- Signed tag `v1.3.2` points to source commit
  `7aa429f477350633bf1aaafe48afd70ce1d88cf7`. Exact-source CI
  [34150172125](https://github.com/aindaco1/podcast-visualizer/actions/runs/34150172125)
  passed core, stable Xcode 26.3, and Xcode 27 preview lanes and attested the
  assembled app for release reuse.
- Release [34150760150](https://github.com/aindaco1/podcast-visualizer/actions/runs/34150760150)
  restored that attested app and passed signing, app/DMG notarization and
  stapling, mounted-image verification, size budgets, checksums, SBOM,
  provenance, and publication.
- All eleven public assets were independently downloaded; all ten checksum
  entries passed. The 230,932,534-byte DMG passed read-only mounted layout,
  code-sign, staple, and Gatekeeper checks. GitHub provenance verification
  confirmed the signed source tag and hosted release workflow. Independent
  Ed25519 verification passed for the feed, full ZIP, and 720,345-byte build-24
  delta using the app's public key.
- The installed 1.3.1/build 24 app completed **Check for Updates**, **Install
  Update**, and **Install and Relaunch**. The installed bundle reports
  1.3.2/build 25; code-sign, staple, and Gatekeeper checks passed. A subsequent
  check confirmed 1.3.2 is newest. Both models remained Ready. The first check
  briefly reported 1.3.1 during publication; retry found the current feed.
  This verifies the manual update flow on the existing account; an automatic
  launch prompt and clean-account acceptance were not established.
- In the installed signed app, a synthetic text click moved the playhead to
  3.059 seconds. A manual adjustment to 4.259 seconds remained authoritative
  for Split at Playhead. Undo restored the original two cues, drag selection
  left the playhead unchanged, and Save preserved the original text, speakers,
  timing, and Checked state after Undo. Active-playback click-to-pause was
  verified separately by the local preview and automated regressions.

## Cleanup and retained development state

Moved eleven obsolete local preview, build, log, and prior-release validation
items (1,644.9 MiB) to a dated Trash folder with a recovery manifest. The
current 1.3.2 public assets and verified 1.3.1 rollback app remain in the local
`PodcastVisualizerReleaseValidation` cache. Compact evidence and the branch
recovery bundle are under `.build/release-validation/1.3.2/`.

Removed five superseded hosted CI artifact records: three unexpired artifacts
totaling 562,740,941 bytes and two expired records. Retained the attested apps
for the current and previous releases. Public release history and the pinned
runtime-bootstrap release remain available.

Archived the merged alignment-runner `chore/ignore-finder-metadata` branch in
a verified Git bundle, then removed its local name. The root checkout has only
main. The Record `codex/speech-progress` branch is retained because its pinned
code is absent from Record main and required by this app. Intentional shared
release branches, development caches/dependencies, runtime, models, fixtures,
and all user projects/media/outputs are preserved. The local development app
was rebuilt with 1.3.2 metadata; the locked Python environment was repaired and
all 27 Python tests passed.

The immutable signed feed embeds the preparation-time notes. This document
and the GitHub release description contain the final acceptance record.
